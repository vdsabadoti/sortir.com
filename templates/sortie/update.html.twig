{% extends 'base.html.twig' %}


{% block body %}


    {{ form_start(form, {'attr' : {'class' : 'w-1/3  mx-auto p-8 border-4 border-slate-400 rounded-lg shadow-lg'}}) }}


    {{ form_widget(form) }}
    {{ form_errors(form, {'attr' : {'class' : 'w-full bg-red-500 text-white'}}) }}

    {{ form_end(form) }}

    <a class="px-6" href="{{ path("app_sortie_publier",{'id' : sortie.id}) }}">
        <button type="button" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
            Publier
        </button>
    </a>



{% endblock %}